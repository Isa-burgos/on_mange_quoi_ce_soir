{% extends 'base.html.twig' %}
{% block title %}Ajouter un repas{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Ajouter un repas pour {{ jour }} – {{ creneau }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.meal_date) }}
        {{ form_row(form.meal_time) }}
        {{ form_row(form.recipe_id) }}
        <button class="btn btn-success">Valider</button>
    {{ form_end(form) }}
</div>
{% endblock %}
